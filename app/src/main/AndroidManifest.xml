<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="followers.githubusersfollowers">
<uses-permission android:name="android.permission.INTERNET"/>
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".GithubUsersFollowersApplication"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.activities.GithubUserFollowersActivity"
            android:label="@string/title_activity_github_users_followers"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.activities.UserNameActivity"
            android:label="@string/title_activity_user_name"
            android:theme="@style/AppTheme.NoActionBar" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>

        <activity
            android:name=".view.activities.GithubUserDetailActivity"
            android:label="@string/title_activity_github_user_detail"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
    </application>

</manifest>